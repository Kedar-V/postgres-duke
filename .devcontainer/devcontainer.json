{
  "name": "IDS706 Postgres Dev",
  "dockerComposeFile": "../week7/docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspaces",
  "forwardPorts": [5432],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql-client && pip install -r week5/requirements.txt",
  "containerEnv": {
    "PGHOST": "db",
    "PGUSER": "vscode",
    "PGPASSWORD": "vscode",
    "PGDATABASE": "duke_sql",
    "PGPORT": "5432"
  }
}
